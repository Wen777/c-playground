#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

#include "greatest.h"

#include "hello.h"

/* A test runs various assertions, then calls PASS(), FAIL(), or SKIP(). */
int return_a_int(void) {
    return 1000;
}
